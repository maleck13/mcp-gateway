apiVersion: v1
kind: Service
metadata:
  labels:
    app: mcp-test
  name: mcp-test
spec:
  ports:
    - name: http
      port: 8081
      protocol: TCP
      targetPort: 8081
  selector:
    app: mcp-test
  type: ClusterIP