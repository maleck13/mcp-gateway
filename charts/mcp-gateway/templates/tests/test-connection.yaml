apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mcp-gateway.fullname" . }}-test-connection"
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "mcp-gateway.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
    "helm.sh/hook-weight": "1"
    "helm.sh/hook-delete-policy": hook-succeeded,hook-failed
spec:
  restartPolicy: Never
  containers:
  - name: test-connection
    image: curlimages/curl:8.5.0
    command: ['sh', '-c']
    args:
      - |
        set -e
        echo "Testing MCP Gateway broker-router connection..."

        # Test broker-router health endpoint
        echo "Checking broker-router health endpoint..."
        curl -f http://mcp-broker-router.{{ .Release.Namespace }}.svc.cluster.local:8080/healthz
        
        echo "All tests passed!"
